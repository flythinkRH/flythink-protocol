{
  "name": "flythink-site",
  "version": "1.0.0",
  "private": true,
  "description": "Flythink static site",
  "scripts": {
    "build": "node scripts/build.js",
    "gates": "node gates/run-gates.js",
    "gates:dist": "node gates/run-gates.js dist"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
